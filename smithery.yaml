build:
  steps:
    - run: npm install
    - run: npm run build

server:
  command: node dist/index.js
  env:
    ADO_ORG: ""

include:
  - dist/
  - package.json
  - package-lock.json

exclude:
  - src/
  - test/
  - node_modules/

